# Diário de Bordo - Projeto Bot IQ Option (Refinamento Estruturado)

**Data:** 25/04/2025 11:24

**Fase Atual:** Início da Fase 3

**Progresso:**
*   **Fase 1 (Organização e Fundação):** Concluída. Diretório `DESENVOLVIMENTO/` confirmado.
*   **Fase 3 (Rastreamento Contínuo):** Iniciada. Criação do arquivo `ROO_STATUS.md`.

**Próximos Passos:**
*   Iniciar a Fase 2 (Análise Profunda e Assimilação).
*   Listar arquivos relevantes (.py, .ini, .env, .md) para análise.
*   **Fase 2 (Análise Profunda):** Iniciada.
    *   Analisado `README.md`: Fornece visão geral do projeto, recursos (API, IA, Modos), configuração (`.env`, `config.ini`), uso (linha de comando `main.py`), arquitetura básica (`ferramental/`, `inteligencia/`) e avisos.
*   Analisado `.env`: Contém as credenciais `IQOPTION_EMAIL` e `IQOPTION_PASSWORD`.
*   Analisado `config.ini`: Arquivo extenso com múltiplas seções ([General], [Credentials], [Trading], [Data], [Learning], [Model], [API], [Logging], [AutoSwitch], [Simulation], [Security], [Monitoring]). Define parâmetros detalhados para modos de operação, trading, IA (RF, NN), gerenciamento de risco, dados, API, logs, segurança e monitoramento. Observada redundância de credenciais com `.env` (mas com flag `use_env_file`).
*   Analisado `main.py`: Orquestrador principal. Gerencia argumentos CLI, logging, configuração (`ConfigManager`), inicialização, conexão API (`Ferramental`), loop principal com lógica para modos (Download, Learning, Test, Real), interação com `Inteligencia` para processamento/treinamento/previsão, execução de ordens via `Ferramental`, tratamento de erros (`ErrorTracker`), rastreamento de performance (`PerformanceTracker`), gerenciamento de risco básico e transição entre modos. Inclui tratamento de sinais e função de cleanup. Pontos de atenção: leitura de credenciais, complexidade do loop principal.
*   Analisado `utils/ConfigManager.py`: Implementa Singleton para gerenciar `config.ini` via `configparser`. Fornece métodos `get_value` (com tipo, default, fallback minúsculo) e `get_list`. Inclui métodos auxiliares para buscar grupos de parâmetros (API, Logging, Risco, etc.). Lógica de credenciais delegada (provavelmente para `main.py` lidar com `.env`).
*   Analisado `utils/Logger.py`: Define `setup_logger` que configura loggers usando `ConfigManager` para obter parâmetros (nível, arquivo, formato, rotação). Permite sobrescrita de nível. Usa `StreamHandler` (console) e `RotatingFileHandler` (arquivo com rotação). Função `get_logger` para obter/criar loggers.
*   Analisado `utils/ErrorTracker.py`: Classe para registrar e gerenciar erros. Armazena lista de erros (com limite), contagem por tipo e lista de erros críticos. Métodos para adicionar, obter, limpar, salvar (JSON) e carregar erros. Integra-se com logging padrão.
*   Analisado `utils/PerformanceTracker.py`: Classe para rastrear desempenho. Armazena lista de trades. Calcula métricas agregadas (win rate, profit, drawdown, etc.), curva de patrimônio e desempenho segmentado (por ativo, timeframe, dia). Métodos para adicionar trades (individual/lote), obter métricas/segmentos, salvar/carregar estado (JSON).
*   Analisado `utils/LogManager.py`: Define classe `LogManager` e função `setup_logger` para configurar logging. Usa `RotatingFileHandler` (arquivo datado) e `StreamHandler` com `colorama` para cores no console. Define nível/método `SUCCESS`. **Observação:** Funcionalidade parece redundante com `utils/Logger.py`, que é o módulo efetivamente usado em `main.py` e integrado ao `ConfigManager`. `LogManager.py` pode ser código legado/não utilizado.
*   Analisado `ferramental/Ferramental.py` (parcial): Classe Singleton que encapsula `iqoptionapi`. Gerencia conexão (c/ 2FA, retry), obtém dados (velas, saldo, indicadores, etc.), executa ordens (`buy` c/ validação de risco pré-execução e verificação pós-execução). Inclui modo de simulação com dados sintéticos (lógica acoplada aos métodos de dados). Trata erros com `ErrorTracker`. Contém métodos obsoletos (digital spot).
*   Analisado `ferramental/Ferramental.py` (final): Contém gerenciamento de streams de velas (real/simulado), obtenção de dados (realtime/histórico c/ fallback simulação), geração de dados sintéticos, e obtenção de resultados de trades (`get_trade_results` - usa `get_optioninfo_v2` na API ou simula baseado em `win_rate`). Confirma obsolescência de métodos digitais. Lógica de simulação fortemente acoplada.
*   Analisado `ferramental/ErrorTracker.py`: Implementação alternativa para rastreamento de erros. Armazena em memória e tenta anexar a JSON (ineficiente). Inclui métodos de análise básica. **Observação:** Parece ser código legado/não utilizado, pois `utils/ErrorTracker.py` é a versão importada e utilizada no restante do código.
*   Analisado `ferramental/PerformanceMetrics.py`: Classe `BotPerformanceMetrics` para rastrear desempenho. Calcula métricas básicas, avançadas (consecutivas), segmentadas (ativo, direção) e temporais. Salva métricas em JSON (c/ timestamp). **Observação:** Parece ser código legado/não utilizado, pois `utils/PerformanceTracker.py` é a versão importada e utilizada no restante do código.
*   Analisado `inteligencia/Inteligencia.py` (parcial): Define classe `Inteligencia` e `LSTMModel` (PyTorch c/ Atenção). Gerencia ciclo de vida do modelo (init, load, save, train, evaluate, predict). Implementa lógica de transição de modo baseada em critérios de desempenho (avaliação do modelo e performance real). Inicia definição de pré-processamento de dados com indicadores técnicos (`pandas_ta`).
*   Analisado `inteligencia/Inteligencia.py` (final): Detalha pipeline de pré-processamento (`preprocess_data`) com adição de múltiplos indicadores técnicos, padrões de candles, features derivadas, normalização (MinMaxScaler) e criação de labels (baseado em retorno futuro). Inclui `process_historical_data` para orquestrar o pipeline e salvar dados processados. Define `setup_test_mode` que carrega modelo ou cria um básico.
*   Analisado `logs/bot.log`: Revela múltiplas execuções falhas. **Problema Crítico:** Falha persistente na conexão inicial com a API IQ Option em todas as tentativas. **Problemas Secundários:** Inconsistências/erros no carregamento de credenciais (TypeError, username None/valor). Consequência: Falha imediata nos modos Teste (sem dados real-time) e Aprendizado (sem dados históricos), impedindo qualquer operação útil. Bot geralmente encerrado manualmente.
*   Analisado `logs/main.log`: Contém logs mais antigos (26/03). Revela erros adicionais de inicialização: dependência `talib` ausente, `TypeError` em credenciais (senha None), `AttributeError` em `Inteligencia.update_auto_switch_criteria`, `TypeError` em `Ferramental.connect` (argumento inesperado), `AttributeError` repetido em `Ferramental.simulation_mode`. Confirma falha de conexão. Sugere histórico de refatorações problemáticas e inconsistências de interface/atributos.
*   Analisado `training_visualizations/inteligencia.log`: Contém logs específicos do módulo `Inteligencia`. Mostra sucesso na atualização de critérios. Registra tentativa de processamento de dados (eurusd) com erro em `_add_technical_indicators` (`AttributeError: 'tuple' object has no attribute 'empty'`, provavelmente em `ta.macd` ou `ta.bbands`), mas o processo continuou e salvou dados processados. Confirma `AttributeError` repetido em `setup_test_mode` (`'Inteligencia' object has no attribute 'models_dir'`, provável erro de digitação para `model_dir`).
*   Analisado `data/processed/eurusd_60_processed.csv` (primeiras linhas): Confirma o output do pipeline de `Inteligencia.py`. Contém OHLCV, metadados, múltiplos indicadores técnicos, padrões de candle, features derivadas e a coluna `label`. Dados numéricos parecem normalizados (MinMaxScaler). Confirma que o processamento de dados foi executado pelo menos uma vez, apesar do erro registrado em `inteligencia.log`.
*   Analisado `error_log.json`: Arquivo está vazio (sem erros registrados), apesar dos múltiplos erros evidentes nos logs de texto (.log). Timestamp corresponde à última execução. **Conclusão:** Indica falha no mecanismo de persistência do `utils/ErrorTracker` ou no fluxo de encerramento/limpeza que deveria salvá-lo. O arquivo não reflete o histórico real de erros.